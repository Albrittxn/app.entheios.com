import { EmptyTab } from "@/components/empty-tab";

export default function UpdatesPage() {
  return (
    <EmptyTab
      title="Updates"
      description="Daily reply-rate digests, A2P deliverability flags, and changelogs across templates and campaigns will land here."
    />
  );
}
