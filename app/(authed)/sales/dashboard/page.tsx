import { EmptyTab } from "@/components/empty-tab";

export default function SalesDashboard() {
  return (
    <EmptyTab
      title="Dashboard"
      description="You'll be able to see every call you've booked and the status of all of them in one view — upcoming, completed, no-shows, closed, follow-ups."
    />
  );
}
