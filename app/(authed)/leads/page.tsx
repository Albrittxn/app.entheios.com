import { EmptyTab } from "@/components/empty-tab";

export default function LeadsHubIndex() {
  return (
    <EmptyTab
      title="Leads"
      description="Master list — upload one large CSV, search and filter every lead in one view. Coming soon."
    />
  );
}
