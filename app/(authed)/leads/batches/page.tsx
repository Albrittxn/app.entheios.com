import { EmptyTab } from "@/components/empty-tab";

export default function LeadsBatches() {
  return (
    <EmptyTab
      title="Batches"
      description="Carve the master list into batches of N — assign to Sales callers, track download + completion. Coming soon."
    />
  );
}
